#!/bin/bash
set -euo pipefail
tool_root="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd -P)"
if ! command -v python3 >/dev/null 2>&1; then
  echo 'Python 3.9+ is required. / Python 3.9以降を準備してください。' >&2
  exit 2
fi
exec python3 "$tool_root/scripts/entry.py" "$@"
